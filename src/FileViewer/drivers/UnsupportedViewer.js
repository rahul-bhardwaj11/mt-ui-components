import React from 'react';

const UnsupportedViewer = () => (
  <div className="unsupported-message">
    <p className="alert">
      <b>File </b> is not supported.
    </p>
  </div>
);

export default UnsupportedViewer;
